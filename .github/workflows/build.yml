name: Build

on:
  push:
    branches: [ main, master ]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest

    # Opt in to Node.js 24 now — all actions/checkout, actions/setup-java,
    # actions/upload-artifact, and softprops/action-gh-release still use
    # Node.js 20 internally, which GitHub is deprecating on June 16 2026.
    # This env var makes the runner execute them on Node.js 24 instead.
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Set version from tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          TAG_VERSION=${GITHUB_REF#refs/tags/v}
          sed -i "s/^version=.*/version=${TAG_VERSION}/" gradle.properties
          echo "Building release version: ${TAG_VERSION}"

      - name: Build with Gradle
        run: ./gradlew shadowJar

      - name: Embed proxy JAR
        run: ./gradlew embedProxyJar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: burp-mcp-all
          path: build/libs/burp-mcp*.jar
          if-no-files-found: error

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/libs/burp-mcp*.jar
          generate_release_notes: true
          name: Release ${{ github.ref_name }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
